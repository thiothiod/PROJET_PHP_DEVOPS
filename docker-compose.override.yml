version: '3.9'

services:
  web:
    ports:
      - "8001:80"  # Alternative port for overrides
    environment:
      - LOG_LEVEL=debug
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  phpmyadmin:
    environment:
      - PMA_ARBITRARY=1

